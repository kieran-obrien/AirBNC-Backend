const formatImages = (imagesData, insertedProperties) => {
  // Create a deep copy
  const copiedImagesData = JSON.parse(JSON.stringify(imagesData));
  for (const image of copiedImagesData) {
   
  }
  return copiedImagesData;
};

module.exports = formatHosts;